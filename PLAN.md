# Plan: ETKDG 嵌入 SIMD 化 → 目标 v1.2.6

## 背景

v1.2.5 后优化阶段已非瓶颈,嵌入主导管线(121 vs 13 ms/构象)。
wasm 已启用 simd128(.cargo/config.toml),但热循环的分支与梯度
散射阻止自动向量化。本轮:把嵌入的最热对循环做手工 SIMD。

## 任务

1. **剖析先行**:原生 debug-info 构建 + sample,定位当前嵌入的
   前几热点行(4D 对循环 vs 3D lr_pairs 循环 vs h_bond vs
   dihedral FD vs 梯度散射)。
2. **选定 1–2 个最热循环改造**:
   - 常量提升:bounds/skip 条件在最小化期间不变 → 一次性过滤
     成扁平 SoA 数组(lo[], hi[], i[], j[]),循环无分支
   - 能量部分:成对 SIMD(f64×2)水平归约;梯度部分若散射受限
     则保持标量或原子主序
   - `cfg(target_feature)` 内联 intrinsic + 标量回退
3. **奇偶策略**:逐位对拍不可行时(求和顺序变化),以系综门禁
   (6/6)与种子测试更新为裁决;先盘点哪些种子测试锁了精确值。
4. **门禁**:cargo test、ensemble 6/6、benchmark 230/230、
   clippy 0、fmt;交错 A/B(原生 + wasm 两条)
5. **发布**:1.2.5→1.2.6(若有可测收益);CODE_STATUS/PLAN

## 验收(实施后实测记录)

**结论:SIMD 路线诚实关闭,零代码变更入库。**

- **实验 1(SLP 可行性,原生 aarch64)**:最小直线形状(纯寄存器
  运算)确实被 SLP 向量化(fsqrt.2d/fmaxnm.2d 出现在产物中)。
- **实验 2(wasm32+simd128)**:同样形状 LLVM **不融合标量链**——
  产物只有 v128.store,无任何 f64x2 算术。安全 Rust 在 wasm 上
  无法触发 SIMD 算术。
- **实验 3(真实循环形状,原生)**:加上坐标 gather(coords[i],
  按对索引)后 SLP 也不再触发;强制 2 路手写的交错 A/B 预热后仅
  1.04–1.06×(首次 2.28× 是冷缓存假象)。逐位一致性已验证
  (assert_eq 通过)。
- **根因**:对循环是 gather/scatter 主导(随机访问 coords[i]、
  grad[i] +=),f64×2 双车道对 gather 成本无改善;RDKit 的同类
  循环也是标量——嵌入差距是结构性的(求值次数、阶段数),
  不是向量宽度。
- **剩余路径**:std::arch intrinsics(需 unsafe,仓库规则禁止;
  若用户明确解禁可重评,但预期收益也仅 ~1.3–1.6× 于该循环,
  占嵌入整体 <15%)。嵌入的进一步收益在减少求值/阶段,非向量化。
