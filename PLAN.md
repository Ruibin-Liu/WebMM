# Plan: 金属二阶残差调查 — 锌角度族 / 铁氰 kb(调查收束,无可实施修复)

## 背景

CODE_STATUS 记录的两个金属二阶残差:锌氨配合物角度 −0.053 Eh(曾归因
"dampa TM rcov,二进制有效 ≈1.22");铁氰化物 kb +0.4%(Hückel pibo
0.971 vs 0.959)。

## 调查过程与发现(实施记录)

1. **参考可靠性危机与排除**:gxtb 二进制的 molfile 读入器有缺陷
   (M CHG 解析崩溃/拓扑误读:ferrocene mol 输入差 +1.01 Eh、cobalt
   直接报错)。**xyz 输入下五个金属夹具参考全部精确复现** → 参考有效,
   此前的"二进制漂移"假象全部来自输入格式。
2. **锌残差精确定位**:逐项对拍 = angle −0.0528 + bond −0.0017;
   逐角对拍(verbose 逐角列表 vs 我们的列表)= 角集合/phi0/FC 几乎
   完全一致(32 vs 31 角,唯一差异一个恰 180.00° 的 H-N-Zn 角,
   FC 差 ≤1.2%)→ 残差集中在 **H-N-Zn 角的 Zn 腿 dampa 阻尼**。
3. **假设逐一检验**:
   - mctc 表版本差(covalentRadD3 = 4/3×Pyykkö2009 金属−10%):我们
     的表与现行 mctc 源**逐位一致**;"金属未缩减"假设被 Ni/Co/Fe
     敏感性实验否决(解除缩减反而劣化 nicarbonyl 0→−8.6e-4 等);
   - dampa 金属侧额外 ×4/3:同上被否;
   - 单一 rcov_Zn 闭合扫描:zinc_ammine 角项闭合需 Zn-N 阻尼和
     ≈1.875 Å(等效 rcov_Zn≈1.165),但同扫描使新鲜独立探针
     [Zn(H2O)6]²⁺(基线残差仅 −0.00031!)劣化 → **无一致单半径解**。
   - 新鲜 [Zn(H2O)6]²⁺ 探针(手工八面体,today 二进制):我们的
     Zn-O 路径基线即近乎精确 → Zn 残差是 Zn-N 腿特异的二进制内部
     行为,不可用一致规则复刻。
4. **锌角度中心角确认**:xtb 的 nb(icase=2)对 nbf-CN>6 的原子删
   空邻居行 → Zn(NH3)4 的 nbf-CN=12 → 无 Zn 中心角;我们的移植
   行为一致(逐角对拍证实)——此前怀疑的"多算 Zn 角"不存在。
5. **铁氰 kb/pibo**:本轮未展开(锌调查耗尽预算);保留原记录。

## 结论与决定

- 锌角度族:定性为**参考二进制的 Zn-N dampa 内部分歧**(源码公式
  与我们的移植逐字一致;mctc 表一致;Ni/Co/Fe/O 路径全部忠实)。
  按"不可裁决的上游分歧"惯例(同 amide dgam 二进制行为先例的
  反向情形——此处无法确定二进制内部用什么),**保持源码忠实实现,
  残差记录为已知项**,不引入魔法常数(单夹具拟合不过独立探针)。
- 关键方法论沉淀(入 CODE_STATUS):**参考生成必须用 xyz 输入**
  (gxtb molfile 读入器不可靠);[Zn(H2O)6]²⁺ 独立探针几何与参考
  已验证可用于未来回归。
- 生产代码零变化(诊断旋钮用后即删);无版本发布。

## 验收

- `cargo test` 273/273、clippy 0、fmt 干净、benchmark 输出与
  v1.1.2 一致(既有 2 离群点)
- 工作区生产代码零 diff;文档(PLAN/CODE_STATUS)更新提交
